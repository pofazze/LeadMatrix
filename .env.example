# Frontend environment variables (Vite requires VITE_ prefix)
# Base URL of the backend API
VITE_API_BASE_URL=http://localhost:4000
# Socket.IO path (must match backend SOCKET_IO_PATH)
VITE_SOCKET_PATH=/socket.io
# Vite dev server port
VITE_PORT=5173
# Optional: webhook proxy target in dev (used by vite.config.js)
VITE_WEBHOOK_TARGET=https://primary-lhz6-production.up.railway.app
